<h1><%= @recipe.name %></h1>

<h2>Created By: <%= link_to @recipe.user.name, user_path(@recipe.user) %></h2>
<h2>Ingredients: </h2>
<ul>
  <% @recipe.ingredients.each do |ingredient| %>
    <li><%= link_to ingredient.name, ingredient_path(ingredient) %></li>
  <% end %>
</ul>
